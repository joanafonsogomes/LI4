@model WebApplication1.Models.Utilizador
@{
    ViewBag.Title = "Enviar email com nova password";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}
<!DOCTYPE html>
<html>

<head>
    <style>
        html * {
            font-family: Montserrat;
        }
    </style>
</head>
<body>
    <br />
    <br />
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-4 col-sm-4 col-xs-12"></div>
            <div class="col-md-4 col-sm-4 col-xs-12">


                @if (!@User.Identity.IsAuthenticated)
                {
                    using (Html.BeginForm("Enviar", "Conta", FormMethod.Post))
                    {

                        @Html.ValidationSummary(true)
                        <form>


                            <!-- Text input-->
                            <div>
                                <center>
                                    <label for="email">Insira email de recuperação de password</label>
                                </center>
                                <div>
                                    <input id="email" name="email" type="text" placeholder="" class="form-control input-md" required="">

                                </div>
                            </div>
                            <br />
                            <!-- Button (Double) -->
                            <div class="form-group">
                                <label class="col-md-4 control-label" for="save"></label>
                                <div class="col-md-8">
                                    <button id="save" name="save" class="btn btn-lg btn-primary btn-block btn-login text-uppercase font-weight-bold mb-2" style="background-color:#CCDBE2; border-color:#11709E; width: fit-content; font-size:13px; color: #11709E;">Enviar</button>
                                </div>
                            </div>


                        </form>
                    }
                }
                <br />
                <br />
                <center>
                    <a href='@Url.Action("Login", "Conta")' class="btn btn-lg btn-primary btn-block btn-login text-uppercase font-weight-bold mb-2" style="background-color:#CCDBE2; border-color:#11709E; width: fit-content; font-size:13px; color: #11709E; margin-left: -6px;">
                    Voltar
                </a>
                </center>
            </div>
            <div class="col-md-4 col-sm-4 col-xs-12"></div>
        </div>
    </div>
</body>
</html>
